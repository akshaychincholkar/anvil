.venv/
backend/.env.local
backend/*.log

# Local SQLite database — never commit (contains real data + OAuth tokens)
backend/anvil.db
backend/anvil.db-wal
backend/anvil.db-shm

# Firebase service-account key — a credential, never commit
*service-account*.json
*serviceAccount*.json
backend/firebase-*.json
firebase-adminsdk-*.json